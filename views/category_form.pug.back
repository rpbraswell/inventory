extends layout

block content

  h1= name

     body

         .form
             h2 Category Form
             form(action="/categories", method="POST")
                 label(for="category") Enter Category Name
                 input#category(type="text", name="category")
                 button(type="submit") Submit


      
